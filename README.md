# Linguistic-Event-Matching
A Python pipeline to isolate stuttering events in fNIRS data and match them with linguistically similar fluent events. Automates "fluent twin" selection using a 5s buffer and multi-factor alignment (grammar, manner, syllables).
